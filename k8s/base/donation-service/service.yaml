apiVersion: v1
kind: Service
metadata:
  name: donation-service
  namespace: solidarytech
  labels:
    app: donation-service
spec:
  selector:
    app: donation-service
  ports:
    - port: 80
      targetPort: 8082
      protocol: TCP
  type: ClusterIP
