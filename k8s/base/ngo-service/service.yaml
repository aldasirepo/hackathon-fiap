apiVersion: v1
kind: Service
metadata:
  name: ngo-service
  namespace: solidarytech
  labels:
    app: ngo-service
spec:
  selector:
    app: ngo-service
  ports:
    - port: 80
      targetPort: 8081
      protocol: TCP
  type: ClusterIP
