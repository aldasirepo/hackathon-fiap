apiVersion: v1
kind: Service
metadata:
  name: volunteer-service
  namespace: solidarytech
  labels:
    app: volunteer-service
spec:
  selector:
    app: volunteer-service
  ports:
    - name: http
      port: 80
      targetPort: 8083
      protocol: TCP
  type: ClusterIP
